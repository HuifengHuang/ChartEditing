你现在要为我初始化一个基于 **Vite + React + TypeScript** 的前端原型工程，目标不是完整系统，而是先做出一个最小可用闭环，用于后续扩展为“D3 图表编辑 + 动态控制面板”系统。

请严格按照下面要求实现。

项目目标：

1. 使用 Vite 创建 React + TypeScript 工程。
2. 页面分为三栏或两栏布局都可以，但至少要有：

   * 左侧或中间：图表预览区
   * 右侧：控制面板区
   * 可选：顶部输入区用于放置后续自然语言意图输入框，但第一版先不用接模型
3. 先内置一个 **D3 柱状图示例**，不要依赖后端，不要接大模型。
4. 用户通过右侧面板修改参数后，图表能够 **实时重新渲染**。
5. 这一步的核心是搭建后续系统骨架，因此代码结构要清晰、模块化，方便后续加入：

   * ChartIR
   * EditIntent
   * PanelSpec
   * PatchPlan
   * LLM 意图解析
   * 动态面板生成

实现范围：

1. 先只做一个柱状图示例。
2. 示例图表使用本地假数据即可，例如：

   * A, B, C, D, E 五个类别
   * 对应数值若干
3. 右侧面板先不要动态生成，先写成静态面板，但面板的字段设计要贴近未来动态面板 schema。
4. 面板至少包含这些控件：

   * chartWidth
   * chartHeight
   * barColor
   * barGap 或 padding
   * titleFontSize
   * titleText
5. 修改这些控件后，D3 图表应立即更新。
6. 页面中要有一个区域显示“当前控制参数 JSON”，方便后续观察状态。

工程要求：

1. 使用 React + TypeScript。
2. 使用函数组件和 hooks。
3. 不要引入状态管理库，先用 React state 即可。
4. D3 请直接使用 d3 包。
5. 样式尽量简单干净，不需要复杂 UI 库；可直接用 CSS module、普通 CSS 或简单内联样式。
6. 代码必须可运行，尽量避免过度设计。
7. 不要写后端，不要写数据库，不要写大模型调用。
8. 不要做 AST 解析，不要做代码 patch，这一步只是先把“面板驱动渲染”的闭环做出来。
9. 组件与目录结构要清晰，建议至少包含：

   * src/components/ChartPreview.tsx
   * src/components/ControlPanel.tsx
   * src/data/sampleData.ts
   * src/types/chart.ts
   * src/App.tsx
10. 在 `src/types/chart.ts` 中先定义一个基础类型，例如：

* ChartConfig
  用来表示当前图表配置。

功能细节：

1. ChartPreview 组件负责：

   * 接收 data 和 config
   * 使用 D3 在 svg 中渲染柱状图
   * 在 config 改变时重新渲染
2. ControlPanel 组件负责：

   * 展示各个表单控件
   * 修改时调用 onChange 更新 config
3. App 组件负责：

   * 维护 config 状态
   * 传递给 ChartPreview 和 ControlPanel
   * 显示当前 config JSON
4. 标题应显示在图表上方。
5. chartWidth、chartHeight 应真正影响 svg 尺寸。
6. barGap/padding 应影响 scaleBand 的 padding。
7. barColor 应影响柱子 fill。
8. titleFontSize 应影响标题字号。

输出要求：

1. 直接生成完整项目代码所需的主要文件内容。
2. 告诉我每个文件应该放什么代码。
3. 如果需要安装依赖，请明确写出命令。
4. 确保我复制后可以直接运行。
5. 最后补一个简短说明，告诉我运行步骤。

额外要求：

1. 代码注释适量即可，不要过多。
2. 保持增量式、可扩展设计。
3. 命名尽量贴近未来论文原型，比如：

   * ChartPreview
   * ControlPanel
   * ChartConfig
4. 这一版先不实现自然语言输入，也不实现动态生成面板，但请在代码结构上为后续扩展预留空间。
